<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/my_nav"
    app:startDestination="@id/mainActivity">
    <activity
        android:id="@+id/orderActivity"
        android:name="com.example.binusezyfoody.OrderActivity"
        android:label="activity_order"
        tools:layout="@layout/activity_order" />
    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.binusezyfoody.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <activity
        android:id="@+id/profileActivity"
        android:name="com.example.binusezyfoody.ProfileActivity"
        android:label="activity_profile"
        tools:layout="@layout/activity_profile" />
</navigation>